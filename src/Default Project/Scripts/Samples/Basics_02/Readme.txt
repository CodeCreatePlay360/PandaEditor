This sample shows basics of how to use a NodePath component, to execute a component they must be attached to a NodePath in scene graph.

Attaching this script to any NodePath will cause it to rotate diagonally around origin, distance from origin is controlled through distance_from_target attribute, if smiley model is loaded in scene it will rotate around the smiley model instead.
